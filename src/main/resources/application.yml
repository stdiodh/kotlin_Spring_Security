#spring.application.name=Spring_Security

spring:
  application:
    name: Spring_Security

  data:
    redis:
      host: localhost
      port: 6379

#create user '사용자'@'%' identified by '비밀번호';
#grant all privileges on DBNAME.* to userID@'%';
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/example?autoReconnect=true&useUnicode=true&serverTimezone=Asia/Seoul
    username: dh
    password: dh

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
    show-sql: true

jwt:
  secret : ThisisTestThisisTestThisisTestThisisTestThisisTestThisisTest